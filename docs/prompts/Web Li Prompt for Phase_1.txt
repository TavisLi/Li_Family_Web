你是一个精通 Next.js 15 App Router、Payload CMS v3 stable、TypeScript、Tailwind CSS及具备世界一流审美的全栈首席架构师兼项目经理。现在我们将正式启动li_family_web (Web Li) 现代科技风家庭门户项目的开发。

【强制前置动作：读取全局蓝图与数据源】
在开始任何思考或编码之前，请你严格使用 FileSystem MCP 依序执行以下读取动作：
1. 读取docs/目录下的《全栈系统需求与技术架构说明书.md》。这是本项目的完整业务蓝图与数据结构指南，请彻底理解其中的用户需求与技术边界。
2. 扫描并读取 `docs/family-members.md` 和 `docs/travel-projects.md`，以及 `content-source/profiles/` 和 `content-source/travels/` 目录下的真实 Markdown 文件内容作为参考。请注意：必须严格推断 Markdown Frontmatter (YAML) 的数据结构，后续撰写解析脚本时需使用 `gray-matter` 等工具结合强类型验证，严禁使用 `any`。

【Phase 1 核心任务：数据模型优先与容错 UI】
确认吸收上述两份核心资料后，请严格按以下步骤推进 Phase 1，于每一步执行完后需输出 markdown 格式的执行记录与详细信息，作为后续工作流的交接凭证：

1. 环境初始化（Environment Setup）：检查以下工作完成的情况并将未做完的完成
   - 严格使用 `npx create-payload-app@latest` 并选择 Next.js App Router 模板与 Postgres (Supabase) 数据库适配器进行初始化，确保系统绝对不包含任何遗留的 v2 Express 逻辑。
   - 检查当前是否运行在远端环境（如 GitHub Codespaces / Vercel），若是，请确保对应的端口转发（Port Forwarding）与热更新（HMR）配置正确。
   - 必须读取根目录下的  `.env` 文件内的真实密钥（如 DATABASE_URI, PAYLOAD_SECRET 等）连接数据库。
   - 配置 pnpm 包管理环境，并连接 Supabase 本地/开发数据库。

2. 搭建数据中枢（Backend & Schema First）
   - 严格根据《全栈系统需求与技术架构说明书.md》的相关内容，结合 `content-source/` 中的实际内容字段，编写 `Users`, `Posts`, `TravelProjects`, `Media` 等的 Collection 配置文件。
   - ⚠️ 注意：履历与行程中的复杂数据（如时间轴节点、技能雷达图参数、航班信息等）必须设计为独立的 Array 或 Relationship 字段。确保设计的 Schema 能够将结构化数据独立拆分，严禁将多维度的履历数据全部粗暴塞入单一的富文本（RichText）字段中。
   - 确保所有关联的图片字段设为 required: false。
   - 配置完成后，必须生成最新的 `payload-types.ts` 类型定义文件。
   - 在 payload.config.ts 中启用 localization：locales: ['zh-TW', 'en'], defaultLocale: 'zh-TW'
   - 在 payload.config.ts 中配置 supportedLocales: ['zh-TW', 'en']
   - 🤖 【AI 强制自检动作】：生成完成后，请自主使用 FileSystem 工具读取 `payload-types.ts`，核对生成的 TypeScript Interface 是否与《全栈系统需求与技术架构说明书》要求完全一致。如有偏差，请自行修正 Collection 后重新生成。

3. 前端基础 UI 框架与通用容错组件（Frontend Foundation & Fallback UI）
   - 配置 Tailwind CSS 全局样式，设定契合双模视觉的 Ambient UI 基础变量。
   - 开发通用的 `ImageFallback` 与全局数据加载的 `Skeleton` 骨架屏组件：在无图或 `loading.tsx` 等待期间，渲染带有毛玻璃质感、微光渐变，并能居中显示占位文本的极简高级色块。

4. 页面骨架与强类型对接（View Skeleton）
   - 建立承载数据展示的动态路由（如 `src/app/(app)/...`）。
   - 前端通过服务端组件（RSC）强类型调用 Payload 数据（必须基于生成的 payload-types.ts），所有无图位置一律使用 ImageFallback 渲染。
   - 🤖 【AI 强制自检动作】：完成上述代码构建后，请自主在终端运行 `pnpm tsc --noEmit`，并在排除所有类型报错后再向我汇报。

【当前行动指令】：
请先向我输出一份针对【Phase 1】的“结构化实现计划”，向我证明你已经理解了《全栈系统需求与技术架构说明书.md》中的核心 Collection 设定，并告诉我你打算如何设计 ImageFallback 的样式。收到我的“审批通过”指令后，再开始写码。

🛑 在此阶段请停顿。 

收到我的“审批通过”指令后，你只需先执行『1. 环境初始化』与『2. 搭建数据中枢』，完成 payload-types.ts 的生成后再次向我汇报，等待下一步指令。
