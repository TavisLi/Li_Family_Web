你是一个精通 Next.js 14+ App Router、Payload CMS v3、TypeScript 及具备世界一流前端设计经验的全栈首席架构师兼项目经理。现在我们将正式启动“Web Li”现代科技风家庭门户项目的开发。

项目的详细需求、开发规范及技术边界，请严格遵守根目录下的《家庭入口网站 - 全栈系统需求与技术架构说明书.md》。

【第一阶段核心任务：基础设施、数据中枢搭建与首批数据容错渲染】
为了提高一次成功率并减少 Token 消耗，我们采用“数据模型优先（Schema-First）”的实施路径，且当前阶段所有多媒体配图均采用“前端优雅占位（Fallback UI）”策略，后续由我在管理后台上传。

【💡 数据源特别提示】：
我已经在项目根目录下创建了 `content-source/` 专属目录。其中：
- `content-source/profiles/` 内存放了  `tavis_resume.md` 和 `lynn_resume.md` 
- `content-source/travels/` 内存放了 `202308东澳全览9日.md` 、`201307海南岛8日.md`和 `202607重庆长江三峡8日.md`
请你在 Phase 1 设计 Payload CMS Collection 模型、字段属性以及分析数据关系时，务必首先前往该目录读取这些文件作为真实的内容参考与类型推导依据。

请多智能体团队严格按照以下四步执行 Phase 1 的开发，于每一步执行完后需输出详细的执行记录与进度等详细信息，可作为其他智能体的输入并接续工作继续完成：

1. 环境初始化（Environment Setup）
   - 基于 Next.js 14 App Router 和 Payload CMS v3 初始化项目。
   - 配置 pnpm 包管理环境，并连接 Supabase 本地/开发数据库。

2. 搭建数据中枢（Backend & Schema First）
   - 严格根据架构说明书第 5 章节，结合 `content-source/` 中的实际内容字段，编写 `Users`, `Posts`, `TravelProjects`, `Media` 的 Collection 配置文件。
   - 注意：所有图片字段（如封面图、图集）必须设为 `required: false`。
   - 配置完成后，必须生成最新的 `payload-types.ts` 类型定义文件。

3. 前端基础 UI 框架与通用容错组件（Frontend Foundation & Fallback UI）
   - 配置 Tailwind CSS 全局样式，设定契合“现代科技风”与“莫兰迪色系”的双模视觉。
   - 开发通用的 `ImageFallback` 组件：若传入的图片数据为空，则渲染一个带有毛玻璃质感、微光渐变，并能居中显示占位文本的极简高级色块。

4. 首批核心页面骨架组装（View Skeleton）
   - 建立动态路由，准备承载 `content-source/` 中读取的文件的数据展示。
   - 前端页面通过服务端组件（RSC）强类型调用 Payload 对应数据，文字内容完整呈现，所有图片位置一律使用步骤3做好的 `ImageFallback` 高级占位组件渲染。

【当前行动指令】：
请不要急于一次性输出所有代码。请首先输出一份针对【Phase 1】的“结构化实现计划与目录变更说明（Artifact）”供我审批。
在计划中，请明确向我指出你将如何设计 `ImageFallback` 组件的视觉样式，如何读取 `content-source/` 目录进行模型参考，以及如何保证 `payload-types.ts` 优先生成。收到我的审批通过指令后，再开始写码。
